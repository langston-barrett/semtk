name: Release
on:
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Build and release SemTK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - shell: bash
        name: Build SemTK
        run: |
          . .env
          # TODO: The tests currently fail in Github Actions.
          mvn --quiet --batch-mode clean install -DskipTests

      - name: Create release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: true
          prerelease: false

      - name: Upload release assets
        id: upload_release_asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: "distribution/target/semtk-opensource-${{ github.ref }}-bin.tar.gz"
          asset_name: semtk-opensource-${{ github.ref }}-bin.tar.gz
          asset_content_type: application/gzip

